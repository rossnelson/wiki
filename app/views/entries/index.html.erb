<%= form_tag "" do %>
  <%= text_field_tag "entry_search", "", :placeholder => "Search Pages", :class => "search" %>
<% end %>


<% @entries.each do |entry| %>

  <div class="entry">
    <%= link_to entry.file_name, entry_path(:id => entry.file_name) %>
  </div>

<% end %>
